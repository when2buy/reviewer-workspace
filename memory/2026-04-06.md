# 2026-04-06

## PR Review — QF-Bench/QuantitativeFinance-Bench#113
- Reviewed task: `execution-ledger-pnl-reconciliation`
- Verdict posted on GitHub: **REQUEST CHANGES**
- Findings:
  - `[CRITICAL]` `tests/test_outputs.py` exposes the full canonical oracle (`_compute_expected()`), so an agent that can read `/tests` can recover the solution logic directly. This breaks the anti-cheating claim and weakens benchmark validity.
  - `[MAJOR]` `tests/test.sh` uses `set -euo pipefail` but checks `$?` only after `pytest`; on test failure the script exits before writing `0` to `/logs/verifier/reward.txt`, so the failure branch is dead code.
- Review submitted with Dongzhikang's QF-Bench PAT.
