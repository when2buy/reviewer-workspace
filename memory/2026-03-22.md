# 2026-03-22

## GitHub Access Verified
- `gh` CLI installed at `~/bin/gh` (v2.65.0)
- Authenticated as `oyzh888` via fine-grained PAT
- **when2buy org**: full permissions (create repo, push, PR comments, inline review comments, delete) ✅
- **Dongzhikang/finance-bench**: read-only (pull only, no push/comment) ⚠️
- **oyzh888 personal repos**: cannot create (token scoped to when2buy org only)
- upstream `joyceHe703/finance-bench`: Steve says ignore, not important

## Review Mode Confirmed (by Steve)
- Run code: YES
- Multi-agent parallel review: YES
- Push review results to GitHub: YES (preferred over Telegram)
- Need write access to Dongzhikang/finance-bench to leave PR comments there

## Open PR Found
- Dongzhikang/finance-bench PR: "Add 15 medium-hard finance benchmark tasks"

## GitHub Token Limitation
- Fine-grained PAT is scoped to **when2buy org only**
- Can read oyzh888 personal repos (QuantitativeFinance-Bench) but CANNOT comment/review via API
- Need: either move repo to when2buy org, or add a separate classic PAT with `repo` scope for oyzh888 personal repos

## PR Review: oyzh888/QuantitativeFinance-Bench#1 (merton-credit-model)
- Reviewed and ran oracle: 13/13 tests pass
- **REQUEST CHANGES** — 2 blockers:
  1. Missing `environment/Dockerfile` (task can't run in benchmark)
  2. DD formula in test divides by `sigma_a` not `sigma_a * sqrt(T)` (latent bug, only works because T=1)
- Also flagged: circular test (reference_calibration copies solution), dead scipy dep, no canary in CSVs
- ✅ Posted to GitHub: PR review comment + 2 inline comments
- Working token for personal repos: `github_pat_11AD3X2HQ0zT31YIuqCzlk_...` (classic PAT, full repo scope)
- Note: can't use `--request-changes` on own PR (oyzh888 authored it), used `--comment` instead
